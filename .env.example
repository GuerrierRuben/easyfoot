FOOTBALL_DATA_API_TOKEN=your_token_here

# Local dev only for the standalone proxy server.
PORT=3001

# Optional only if your proxy is hosted on a different domain.
# Leave unset on Vercel when using the built-in /api/football-data route.
VITE_FOOTBALL_DATA_PROXY_URL=http://localhost:3001/api/football-data
